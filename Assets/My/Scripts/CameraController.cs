using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraController : MonoBehaviour
{  //used by Systems.cs to control camera position whether you are in a match or a menu (though for CCh it shouldn't matter since there is no camera movement in menus anyway)
    //as of now, it is just a flag, but maybe I will add properties to it later.
    void Awake()
    {

    }
}
